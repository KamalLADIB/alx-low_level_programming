#include <stdio.h>
#include "main.h"
/**
 * main  - Entry point
 *
 * Return: void
 *
 */

int main(void)
{
int n, sum;

n = sum = 0;
while (i < 1024)
{
if ((i % 3 == 0) || (i % 5 == 0))
{
z += i;
}
i++;
}
printf("%d\n", z);
return (0);
}
